# 🌱 Green Agro Farm - Web Application (MERN Stack)

এটি একটি ডেইরি এবং মিষ্টির দোকানের ম্যানেজমেন্ট সিস্টেম। এখানে নতুন প্রোডাক্ট অ্যাড করা, ডিলিট করা এবং লাইভ আপডেট দেখার ব্যবস্থা আছে।

## 🚀 যা যা কাজ শেষ হয়েছে (Completed Features)

### ১. ব্যাকএন্ড (Backend) - Node.js & Express
- [x] সার্ভার সেটআপ করা হয়েছে (Express.js)।
- [x] ডাটাবেস কানেকশন (MongoDB Atlas) সফল হয়েছে।
- [x] **API তৈরি:**
    - `GET /products` -> সব প্রোডাক্ট দেখার জন্য।
    - `POST /products` -> নতুন প্রোডাক্ট অ্যাড করার জন্য।
    - `DELETE /products/:id` -> প্রোডাক্ট ডিলিট করার জন্য।
- [x] **CORS ফিক্স:** ফ্রন্টএন্ড থেকে ডাটা নেওয়ার পারমিশন দেওয়া হয়েছে।
- [x] **Deployment:** সার্ভার এখন Render-এ লাইভ চলছে।

### ২. ফ্রন্টএন্ড (Frontend) - React (Vite)
- [x] রিয়্যাক্ট প্রজেক্ট সেটআপ করা হয়েছে।
- [x] লাইভ সার্ভার (Render) থেকে ডাটা ফেচ (Fetch) করে ওয়েবসাইটে দেখানো হয়েছে।
- [x] **স্মার্ট UI ডিজাইন:**
    - প্রোডাক্টের নাম লিখলেই অটোমেটিক আইকন (যেমন: দুধ 🥛, দই 🥣) চলে আসে।
    - গোল্ডেন এবং গ্রীন কালার থিম (মিষ্টির দোকানের মতো)।
- [x] প্রোডাক্ট ডিলিট বাটন (Delete) কাজ করছে।

---

## 🛠️ প্রযুক্তি যা ব্যবহার করা হয়েছে (Tech Stack)
- **Frontend:** React.js, Vite
- **Backend:** Node.js, Express.js
- **Database:** MongoDB Atlas
- **Live Server:** Render

---

## 🏃‍♂️ প্রজেক্ট রান করার নিয়ম

যেহেতু ব্যাকএন্ড লাইভ আছে, তাই শুধু ফ্রন্টএন্ড রান করলেই হবে।

১. টার্মিনাল ওপেন করে `client` ফোল্ডারে ঢুকতে হবে:
   `cd client`

২. অ্যাপ রান করতে হবে:
   `npm run dev`

৩. ব্রাউজারে লিংক ওপেন করতে হবে:
   `http://localhost:5173`
